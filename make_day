#!/bin/zsh

# Check if an argument is supplied
if [ -z "$1" ]; then
  echo "Usage: make_day <number>"
  exit 1
fi

# Create the directory with the prefix "day"
dir_name="day$1"
mkdir -p "$dir_name" && echo "Directory '$dir_name' created successfully."
